/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DeviceattributetypeService } from './deviceattributetype.service';

describe('Service: Deviceattributetype', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DeviceattributetypeService]
    });
  });

  it('should ...', inject([DeviceattributetypeService], (service: DeviceattributetypeService) => {
    expect(service).toBeTruthy();
  }));
});
