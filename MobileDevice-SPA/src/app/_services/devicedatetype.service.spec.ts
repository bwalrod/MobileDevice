/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DevicedatetypeService } from './devicedatetype.service';

describe('Service: Devicedatetype', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DevicedatetypeService]
    });
  });

  it('should ...', inject([DevicedatetypeService], (service: DevicedatetypeService) => {
    expect(service).toBeTruthy();
  }));
});
